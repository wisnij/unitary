const buildMetadata = String.fromEnvironment(
  'BUILD_METADATA',
  defaultValue: '',
);

/// The project site, where the README, web app, and privacy policy are
/// published.
const projectHomeUrl = 'https://unitary.wisnij.dev/';

/// The GitHub repository holding the app's source code.
const sourceCodeUrl = 'https://github.com/wisnij/unitary';
